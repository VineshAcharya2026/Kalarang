[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA routing — all paths serve index.html for React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
